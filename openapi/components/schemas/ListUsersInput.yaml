allOf:
  - $ref: ./PageInput.yaml
  - type: object
    properties:
      status:
        type: string
        enum:
          - active
          - inactive
          - suspended
        description: 按状态筛选用户
        example: active
      keyword:
        type: string
        description: 搜索关键词，用于搜索用户名或邮箱
        example: john
