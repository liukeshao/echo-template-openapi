type: object
properties:
  username:
    type: string
    minLength: 3
    maxLength: 50
    description: 用户名，3-50个字符，可选
    example: john_smith
  email:
    type: string
    format: email
    description: 用户邮箱，可选
    example: john.smith@example.com
description: 更新当前用户信息的输入参数 