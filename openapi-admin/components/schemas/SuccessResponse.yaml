allOf:
  - $ref: ./BaseResponse.yaml
  - type: object
    required:
      - data
    properties:
      code:
        type: integer
        enum: [0]
        description: 成功状态码，固定为0
      data:
        description: 成功时的业务数据
