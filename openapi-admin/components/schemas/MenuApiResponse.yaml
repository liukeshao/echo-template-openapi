oneOf:
  - $ref: ./MenuSuccessResponse.yaml
  - $ref: ./ErrorResponse.yaml
discriminator:
  propertyName: code
description: |
  菜单相关API的统一响应格式
  - 成功时(code=0)：data包含菜单信息
  - 失败时(code!=0)：data为null，errors包含错误详情